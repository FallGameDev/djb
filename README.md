# djb
DJB Language Playground is a browser-based custom language editor that renders text, shapes, buttons, images, and backgrounds in real time. It supports .djb save/load files, rules documentation, and exporting projects as standalone HTML apps using a built-in JavaScript interpreter.
